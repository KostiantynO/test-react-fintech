import { Container } from 'common';
import scss from './Footer.module.scss';

export const Footer = () => {
  return (
    <footer className={scss.Footer}>
      <Container></Container>
    </footer>
  );
};
